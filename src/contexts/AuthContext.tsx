
import { useAuth } from '@/hooks/useAuth';

// Re-export the AuthProvider from the hook file for backwards compatibility
export { AuthProvider } from '@/hooks/useAuth';

// Re-export the useAuth hook as well
export { useAuth };
